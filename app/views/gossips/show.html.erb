
<style>

.jumbotron {
  height: 420px;
  background-image: url("https://x96.com/wp-content/uploads/2019/02/secret.png");
  background-repeat: no-repeat;
  background-size:cover;
  background-attachment:fixed;
}

</style>


<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 style="margin-top:100px; font-size:58px; color:white;" class="display-4"><strong>Le GOSSIP :</strong> <%= @gossip.find(params[:id]).title%></h1>
    <p style="font-size:28px; color:white;" class="lead">Tu trouveras ci-dessous toutes les infos relatives au Gossip qui t'intéresse espèce de stalker.</p>
  </div>
</div>


<center>
<div class="card" style="width:60%;">
  <div class="card-header">
    <p><strong>Titre : </strong><%= @gossip.find(params[:id]).title%></p>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p><strong>Gossip : </strong><%= @gossip.find(params[:id]).content%></p>
      <footer class="blockquote-footer"><strong>Autheur :</strong> <% g = @gossip.find(params[:id]) %> <% h = g.user_id %> <%= @user.find(h).first_name%> <%= @user.find(h).last_name%> <cite title="Source Title"><strong>
      <br>Créé le :</strong> <%= @gossip.find(params[:id]).created_at%></cite></footer>
    </blockquote>
  </div>
</div>
</center>


<div style=" margin-top:28px; text-align:center;">

<% if current_user && (session[:user_id] == @gossip.find(params[:id]).user_id) %>

  <a  href="<%=edit_gossip_path%>" class="btn btn-outline-primary" > Modifier le gossip </a>
  <a href="<%= gossip_path%>" class="btn btn-outline-danger" data-confirm="Êtes vous sûr de vouloir supprimer ce gossip?" data-method="DELETE">Supprimer</a>
<% end %>

</div>


<br>


<center>

  <a href="<%=author_path(h)%>" class="btn btn-outline-light" style="border-color:#62DDB9; color:#62DDB9;"> Lien vers la page du Gossipeur </a>
  <a href="<%=city_path(@user.find(h).city_id)%>" class="btn btn-outline-light" style="border-color:#62DDB9; color:#62DDB9;"> Lien vers la ville du Gossipeur </a>
  <br><br>

</center>
